using UnityEngine;
using System.Collections;

public class MasterServerScript : MonoBehaviour {

	/*private bool received = false;
	public string IP = "127.0.0.1";
	public int Port = 25001;

	[SerializeField]
	GameObject mainMenuPrefab;
	GameObject thisMainMenu;

	public MenuScript mainMenu;

	void Start() {

		thisMainMenu = Instantiate(mainMenuPrefab, new Vector3(0,0,0), Quaternion.identity) as GameObject;
		mainMenu = thisMainMenu.GetComponent<MenuScript>();
	}

	void Update()
	{
		bool hasToStart = true;

		for(int i = 0; i< mainMenu.playerPrefabs.Count;++i)
		{
			if(!(mainMenu.IsthisSkillchosen[i]))
			{
				hasToStart = false;
			}
		}

		if(hasToStart)
		{
			
		}
	}

	void OnGUI() {
		if(GUI.Button(new Rect(100,100,100,100), "Start the server"))
		{
			
		}
	}*/
}
